<?php
/**
 * Migration: Create notifications table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_102213
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateNotificationsTable_2025_12_11_102213
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `notifications` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `user_id` int(11) NOT NULL COMMENT 'ID del usuario destinatario',
  `user_type` enum('admin','super_admin') NOT NULL COMMENT 'Tipo de usuario',
  `type` varchar(100) NOT NULL COMMENT 'Tipo de notificación',
  `title` varchar(255) NOT NULL COMMENT 'Título',
  `message` text NOT NULL COMMENT 'Mensaje',
  `data` text DEFAULT NULL COMMENT 'Datos adicionales (JSON)',
  `is_read` tinyint(1) DEFAULT '0' COMMENT '1 = Leída, 0 = No leída',
  `read_at` datetime DEFAULT NULL COMMENT 'Fecha de lectura',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `user_type_id` (`user_type`, `user_id`),
  KEY `is_read` (`is_read`),
  KEY `created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE notifications (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL,
  user_type VARCHAR(20) NOT NULL,
  "type" VARCHAR(100) NOT NULL,
  title VARCHAR(255) NOT NULL,
  message TEXT NOT NULL,
  "data" TEXT,
  is_read SMALLINT DEFAULT '0',
  read_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CHECK (user_type IN ('admin', 'super_admin'))
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX notifications_user_type_id ON notifications(user_type, user_id)");
            $pdo->exec("CREATE INDEX notifications_is_read ON notifications(is_read)");
            $pdo->exec("CREATE INDEX notifications_created_at ON notifications(created_at)");

            // Add comments
            $pdo->exec("COMMENT ON COLUMN notifications.user_id IS 'ID del usuario destinatario'");
            $pdo->exec("COMMENT ON COLUMN notifications.user_type IS 'Tipo de usuario'");
            $pdo->exec("COMMENT ON COLUMN notifications."type" IS 'Tipo de notificación'");
            $pdo->exec("COMMENT ON COLUMN notifications.title IS 'Título'");
            $pdo->exec("COMMENT ON COLUMN notifications.message IS 'Mensaje'");
            $pdo->exec("COMMENT ON COLUMN notifications."data" IS 'Datos adicionales (JSON)'");
            $pdo->exec("COMMENT ON COLUMN notifications.is_read IS '1 = Leída, 0 = No leída'");
            $pdo->exec("COMMENT ON COLUMN notifications.read_at IS 'Fecha de lectura'");
        }

        echo "✓ Table notifications created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `notifications`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS notifications");
        }

        echo "✓ Table notifications dropped\n";
    }
}
