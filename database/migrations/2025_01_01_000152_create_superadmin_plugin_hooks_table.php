<?php
/**
 * Migration: Create superadmin_plugin_hooks table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_102306
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateSuperadminPluginHooksTable_2025_12_11_102306
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `superadmin_plugin_hooks` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `plugin_id` int(11) NOT NULL COMMENT 'ID del plugin',
  `hook_name` varchar(255) NOT NULL COMMENT 'Nombre del hook/acción',
  `callback` varchar(500) NOT NULL COMMENT 'Función callback',
  `priority` int(11) DEFAULT '10' COMMENT 'Prioridad de ejecución (menor = primero)',
  `is_active` tinyint(1) DEFAULT '1' COMMENT '1 = Activo, 0 = Inactivo',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `plugin_id` (`plugin_id`),
  KEY `hook_name` (`hook_name`),
  KEY `is_active` (`is_active`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE superadmin_plugin_hooks (
  id SERIAL PRIMARY KEY,
  plugin_id INTEGER NOT NULL,
  hook_name VARCHAR(255) NOT NULL,
  callback VARCHAR(500) NOT NULL,
  priority INTEGER DEFAULT '10',
  is_active SMALLINT DEFAULT '1',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX superadmin_plugin_hooks_plugin_id ON superadmin_plugin_hooks(plugin_id)");
            $pdo->exec("CREATE INDEX superadmin_plugin_hooks_hook_name ON superadmin_plugin_hooks(hook_name)");
            $pdo->exec("CREATE INDEX superadmin_plugin_hooks_is_active ON superadmin_plugin_hooks(is_active)");

            // Add comments
            $pdo->exec("COMMENT ON COLUMN superadmin_plugin_hooks.plugin_id IS 'ID del plugin'");
            $pdo->exec("COMMENT ON COLUMN superadmin_plugin_hooks.hook_name IS 'Nombre del hook/acción'");
            $pdo->exec("COMMENT ON COLUMN superadmin_plugin_hooks.callback IS 'Función callback'");
            $pdo->exec("COMMENT ON COLUMN superadmin_plugin_hooks.priority IS 'Prioridad de ejecución (menor = primero)'");
            $pdo->exec("COMMENT ON COLUMN superadmin_plugin_hooks.is_active IS '1 = Activo, 0 = Inactivo'");
        }

        echo "✓ Table superadmin_plugin_hooks created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `superadmin_plugin_hooks`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS superadmin_plugin_hooks");
        }

        echo "✓ Table superadmin_plugin_hooks dropped\n";
    }
}
