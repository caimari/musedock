<?php
/**
 * Migration: Create custom_form_settings table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_102305
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateCustomFormSettingsTable_2025_12_11_102305
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `custom_form_settings` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT,
  `tenant_id` int(11) unsigned DEFAULT NULL COMMENT 'NULL = configuración global',
  `setting_key` varchar(100) NOT NULL,
  `setting_value` text DEFAULT NULL,
  `setting_type` enum('string','int','bool','json','array') DEFAULT 'string',
  `created_at` datetime DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `unique_setting_tenant` (`setting_key`, `tenant_id`),
  KEY `idx_tenant_id` (`tenant_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE custom_form_settings (
  id SERIAL PRIMARY KEY,
  tenant_id INTEGER,
  setting_key VARCHAR(100) NOT NULL,
  setting_value TEXT,
  setting_type VARCHAR(20) DEFAULT 'string',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  CHECK (setting_type IN ('string', 'int', 'bool', 'json', 'array')),
  UNIQUE (setting_key, tenant_id)
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX custom_form_settings_idx_tenant_id ON custom_form_settings(tenant_id)");

            // Add comments
            $pdo->exec("COMMENT ON COLUMN custom_form_settings.tenant_id IS 'NULL = configuración global'");
        }

        echo "✓ Table custom_form_settings created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `custom_form_settings`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS custom_form_settings");
        }

        echo "✓ Table custom_form_settings dropped\n";
    }
}
