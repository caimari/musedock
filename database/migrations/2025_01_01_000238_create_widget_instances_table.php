<?php
/**
 * Migration: Create widget_instances table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_102306
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateWidgetInstancesTable_2025_12_11_102306
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `widget_instances` (
  `id` int(10) unsigned NOT NULL AUTO_INCREMENT,
  `tenant_id` int(10) unsigned DEFAULT NULL,
  `theme_slug` varchar(50) NOT NULL,
  `area_slug` varchar(50) NOT NULL,
  `widget_slug` varchar(50) NOT NULL,
  `position` int(10) unsigned NOT NULL DEFAULT '0',
  `config` text DEFAULT NULL,
  `created_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `unique_widget_instance` (`tenant_id`, `theme_slug`, `area_slug`, `position`),
  KEY `idx_widget_instance_tenant` (`tenant_id`),
  KEY `idx_widget_instance_area` (`theme_slug`, `area_slug`),
  KEY `idx_widget_instance_lookup` (`tenant_id`, `theme_slug`, `area_slug`, `position`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE widget_instances (
  id SERIAL PRIMARY KEY,
  tenant_id INTEGER,
  theme_slug VARCHAR(50) NOT NULL,
  area_slug VARCHAR(50) NOT NULL,
  widget_slug VARCHAR(50) NOT NULL,
  "position" INTEGER NOT NULL DEFAULT '0',
  config TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE (tenant_id, theme_slug, area_slug, "position")
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX widget_instances_idx_widget_instance_tenant ON widget_instances(tenant_id)");
            $pdo->exec("CREATE INDEX widget_instances_idx_widget_instance_area ON widget_instances(theme_slug, area_slug)");
            $pdo->exec("CREATE INDEX widget_instances_idx_widget_instance_lookup ON widget_instances(tenant_id, theme_slug, area_slug, \"position\")");
        }

        echo "✓ Table widget_instances created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `widget_instances`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS widget_instances");
        }

        echo "✓ Table widget_instances dropped\n";
    }
}
