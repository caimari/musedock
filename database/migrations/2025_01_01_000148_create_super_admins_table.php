<?php
/**
 * Migration: Create super_admins table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_091120
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateSuperAdminsTable_2025_12_11_091120
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `super_admins` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(100) DEFAULT NULL,
  `email` varchar(255) NOT NULL,
  `password` varchar(255) NOT NULL,
  `avatar` varchar(255) DEFAULT NULL,
  `role` varchar(50) DEFAULT 'superadmin',
  `is_root` tinyint(1) DEFAULT '0',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` datetime DEFAULT NULL,
  `two_factor_enabled` tinyint(1) DEFAULT '0',
  `two_factor_secret` varchar(64) DEFAULT NULL,
  `two_factor_recovery_codes` text DEFAULT NULL,
  `two_factor_enabled_at` datetime DEFAULT NULL,
  `two_factor_last_used_at` datetime DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `email` (`email`),
  KEY `idx_email` (`email`),
  KEY `idx_super_admins_2fa` (`two_factor_enabled`),
  KEY `idx_super_admins_is_root` (`is_root`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE super_admins (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(255) NOT NULL,
  password VARCHAR(255) NOT NULL,
  avatar VARCHAR(255),
  role VARCHAR(50) DEFAULT 'superadmin',
  is_root SMALLINT DEFAULT '0',
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP,
  two_factor_enabled SMALLINT DEFAULT '0',
  two_factor_secret VARCHAR(64),
  two_factor_recovery_codes TEXT,
  two_factor_enabled_at TIMESTAMP,
  two_factor_last_used_at TIMESTAMP,
  UNIQUE (email)
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX idx_email ON super_admins(email)");
            $pdo->exec("CREATE INDEX idx_super_admins_2fa ON super_admins(two_factor_enabled)");
            $pdo->exec("CREATE INDEX idx_super_admins_is_root ON super_admins(is_root)");
        }

        echo "✓ Table super_admins created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `super_admins`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS super_admins");
        }

        echo "✓ Table super_admins dropped\n";
    }
}
