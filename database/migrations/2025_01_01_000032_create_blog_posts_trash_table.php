<?php
/**
 * Migration: Create blog_posts_trash table
 * Generated by MuseDock Dual Migration Generator
 * Generated at: 2025_12_11_102305
 * Compatible with: MySQL/MariaDB + PostgreSQL
 */

use Screenart\Musedock\Database;

class CreateBlogPostsTrashTable_2025_12_11_102305
{
    public function up()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("
                CREATE TABLE `blog_posts_trash` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `post_id` int(11) NOT NULL,
  `tenant_id` int(11) DEFAULT NULL,
  `deleted_by` int(11) NOT NULL,
  `deleted_by_name` varchar(100) DEFAULT NULL COMMENT 'Cache del nombre',
  `deleted_by_type` enum('user','admin','superadmin') DEFAULT 'admin',
  `deleted_at` datetime NOT NULL,
  `scheduled_permanent_delete` datetime DEFAULT NULL COMMENT 'Auto-eliminar después de 30 días',
  `reason` varchar(255) DEFAULT NULL COMMENT 'Razón de eliminación (opcional)',
  `ip_address` varchar(45) DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_post_id` (`post_id`),
  KEY `idx_tenant_id` (`tenant_id`),
  KEY `idx_deleted_at` (`deleted_at`),
  KEY `idx_scheduled_delete` (`scheduled_permanent_delete`),
  KEY `idx_deleted_by` (`deleted_by`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
            ");
        } else {
            // PostgreSQL
            $pdo->exec("
                CREATE TABLE blog_posts_trash (
  id SERIAL PRIMARY KEY,
  post_id INTEGER NOT NULL,
  tenant_id INTEGER,
  deleted_by INTEGER NOT NULL,
  deleted_by_name VARCHAR(100),
  deleted_by_type VARCHAR(20) DEFAULT 'admin',
  deleted_at TIMESTAMP NOT NULL,
  scheduled_permanent_delete TIMESTAMP,
  reason VARCHAR(255),
  ip_address VARCHAR(45),
  CHECK (deleted_by_type IN ('user', 'admin', 'superadmin'))
)
            ");

            // Create indexes
            $pdo->exec("CREATE INDEX blog_posts_trash_idx_post_id ON blog_posts_trash(post_id)");
            $pdo->exec("CREATE INDEX blog_posts_trash_idx_tenant_id ON blog_posts_trash(tenant_id)");
            $pdo->exec("CREATE INDEX blog_posts_trash_idx_deleted_at ON blog_posts_trash(deleted_at)");
            $pdo->exec("CREATE INDEX blog_posts_trash_idx_scheduled_delete ON blog_posts_trash(scheduled_permanent_delete)");
            $pdo->exec("CREATE INDEX blog_posts_trash_idx_deleted_by ON blog_posts_trash(deleted_by)");

            // Add comments
            $pdo->exec("COMMENT ON COLUMN blog_posts_trash.deleted_by_name IS 'Cache del nombre'");
            $pdo->exec("COMMENT ON COLUMN blog_posts_trash.scheduled_permanent_delete IS 'Auto-eliminar después de 30 días'");
            $pdo->exec("COMMENT ON COLUMN blog_posts_trash.reason IS 'Razón de eliminación (opcional)'");
        }

        echo "✓ Table blog_posts_trash created\n";
    }

    public function down()
    {
        $pdo = Database::connect();
        $driver = $pdo->getAttribute(PDO::ATTR_DRIVER_NAME);

        if ($driver === 'mysql') {
            $pdo->exec("DROP TABLE IF EXISTS `blog_posts_trash`");
        } else {
            $pdo->exec("DROP TABLE IF EXISTS blog_posts_trash");
        }

        echo "✓ Table blog_posts_trash dropped\n";
    }
}
