<?php

/**
 * Festival Directory — Bootstrap
 * Plugin exclusivo para tenant festivalnews.org (tenant_id: 53)
 *
 * Note: Admin sidebar menus are registered in tenant_menus (DB)
 * automatically when the plugin is activated via /admin/plugins.
 * See plugin.json > admin_menu for the menu definition.
 */

if (defined('BOOTSTRAP_FESTIVAL_DIRECTORY')) return;
define('BOOTSTRAP_FESTIVAL_DIRECTORY', true);
